import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Stories = ({story}) => {
  return (
    <View styles={styles.storyContainer}>
      
      
    </View>
  )
}

export default Stories

const styles = StyleSheet.create({
  storyContainer:{
    backgroundColor:'white',
    borderRadius:50,
    width:50,
    height:97
  },
})